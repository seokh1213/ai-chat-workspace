# Frontend Template

새 프로젝트의 React frontend를 생성할 때 이 템플릿을 기준으로 시작한다.

초기 구현 목표:

- workspace select screen
- data space editor shell
- generic source record panel
- chat session list
- active chat view
- Markdown streaming renderer
- provider settings UI

다음 AI Agent는 `templates/docs/04_FRONTEND_BLUEPRINT.md`를 따른다.
